---
import CardProyectos from "./CardProyectos.astro";
import {proyectoData} from "../data/proyecto";
---

<div id="proyectos" class="w-full overflow-hidden bg-gradient-to-b from-[#3d0370] to-[#9859d0] border-b-3 border-black text-4xl flex justify-center px-4 md:px-0">
  <div class="w-full max-w-6xl mt-16">
    <h2 class="text-4xl md:text-7xl text-center font-bold bg-gradient-to-b from-[#ff54f6] to-[#ffffff] text-transparent bg-clip-text py-4 underline">
      Proyectos&lt;/&gt;
    </h2>
    <div class="mt-12 md:mt-20 mb-10 flex flex-wrap gap-6 md:gap-8 justify-center md:justify-between">
      {
        proyectoData.map((proyectoItem) => (
          <CardProyectos 
            title={proyectoItem.title} 
            descripcion={proyectoItem.descripcion} 
            imagen={proyectoItem.imagen} 
            tecnologias={proyectoItem.tecnologias}
            github={proyectoItem.github}
            enlace={proyectoItem.enlace}
          />
        ))
      }
    </div>

    <div class="text-2xl md:text-2xl text-white mt-12 mb-16 flex justify-center font-semibold">
      <p class="hover:scale-110 cursor-pointer transition-transform duration-300">Ver más <span class="text-amber-300 ml-2">🡢</span></p>
    </div>
  </div>
</div>
